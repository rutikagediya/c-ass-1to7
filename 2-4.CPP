#include<iostream.h>
#include<conio.h>
inline int rectanglearea(int length,int width)
{
return length*width;
}
int main()
{
clrscr();
int length,width;
cout<<"enter the length of the rectangle:");
cin>>length;
cout<<"enter the width of the rectangle:");
cin>>width;
cout<<"the area of rectangle is:"<<rectangle is:"<<rectanglearea(length,width)<<std:endl;
getch();
return 0;
}